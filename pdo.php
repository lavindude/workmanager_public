<?php
    //heroku postgre server:

    // $pdo = new PDO(*****);
    // $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);

    //localhost:

    $pdo = new PDO(******);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
